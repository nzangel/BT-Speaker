name: Build & Publish Add-on

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        arch:
          - amd64
          - aarch64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: QEMU (cross-compilation ARM)
        uses: docker/setup-qemu-action@v3

      - name: Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extraire la version sans le "v"
        id: version
        run: echo "VERSION=$(echo $GITHUB_REF | sed 's/refs\/tags\/v//')" >> $GITHUB_OUTPUT

      # Vérification — visible dans les logs Actions
      - name: Vérifier la version extraite
        run: echo "Version = ${{ steps.version.outputs.VERSION }}"

      - name: Build & Push ${{ matrix.arch }}
        uses: docker/build-push-action@v5
        with:
          context: ./bt_speaker_addon
          push: true
          platforms: linux/${{ matrix.arch }}
          build-args: |
            BUILD_FROM=ghcr.io/home-assistant/${{ matrix.arch }}-base:latest
          tags: |
            ghcr.io/${{ github.repository_owner }}/bt-speaker-addon-${{ matrix.arch }}:${{ steps.version.outputs.VERSION }}
            ghcr.io/${{ github.repository_owner }}/bt-speaker-addon-${{ matrix.arch }}:latest